## This is a setup for an R based assistant

- `tasks/tasks.csv` --- `.csv` file with tasks. Possible fields are:
  - `id` --- id number. Should be different for different tasks.
  - `task`
  - `skill`
  - `schedule`
  - `ignore`
  - `params`

- `skills` --- possible commands
